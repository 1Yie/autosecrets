# Package the Python Agent as a self-contained executable

The Agent will be authored in Python but released as signed, self-contained executables for supported Linux architectures. This keeps the Agent implementation in Python without depending on or modifying each Managed Node's system Python environment.
