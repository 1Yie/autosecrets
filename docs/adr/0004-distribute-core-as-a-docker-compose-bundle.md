# Distribute Core as a Docker Compose bundle

The supported first-release installation will be a versioned Docker Compose bundle containing Core, PostgreSQL, and optional Caddy-based HTTPS termination. This provides one tested self-hosted path while still allowing operators to supply an external PostgreSQL instance or reverse proxy.
